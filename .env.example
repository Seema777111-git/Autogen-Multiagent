# Copy this file to .env and fill in real values.
# NOTE: this app's config.py uses local Ollama (no API key needed
# for the model itself). Only add keys here if you actually use them
# elsewhere in the project.
# OPENAI_API_KEY=
